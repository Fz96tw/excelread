global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'iaconnector'
    scrape_interval: 15s
    metrics_path: '/metrics'
    params:
      format: ['prometheus']
    static_configs:
      - targets: ['trinket.cloudcurio.com']  # use this instead of localhost
